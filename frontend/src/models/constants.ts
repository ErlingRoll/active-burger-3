export enum Rarity {
    COMMON = "common",
    RARE = "rare",
    EPIC = "epic",
    LEGENDARY = "legendary",
    ARTIFACT = "artifact",
}

export enum TileObjectType {
    EXIT = "exit",
    CHEST = "chest",
}

export enum RunOptionType {
    LOOT = "loot",
}

export enum LootType {
    GOLD = "gold",
    ESSENCE = "essence",
}
