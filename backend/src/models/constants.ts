export enum Rarity {
    COMMON = "common",
    RARE = "rare",
    EPIC = "epic",
    LEGENDARY = "legendary",
    ARTIFACT = "artifact",
}

export enum TileObjectType {
    CHEST = "chest",
    EXIT = "exit",
}
